extends ../layouts/default


block content
	.container: .jumbotron
		h1 shimono

		ul
			each entry in entries
				li
					h3 #{entry.title}
					p !{entry.content}
					img(src=`uploads/${entry.img.filename}` width="50px")